# Rollout phases

## Phase 1: Read-only
- Deliver WP plugin settings page and shortcode.
- Show events and venues from API in frontend.
- Add source sync status endpoint and admin visibility.

## Phase 2: RSVP counts
- Enable anonymous RSVP writes.
- Display aggregate counts per event.
- Add rate limiting and abuse monitoring.

## Phase 3: Seed recovery and sync
- Force seed backup confirmation during onboarding.
- Add restore flow and encrypted data sync.
- Add migration/versioning for local encrypted state.

## Phase 4: Notifications and channels
- Add push subscription token handling.
- Add segmented notification tags (city/category).
- Add optional channel/chat module behind feature flag.
